import * as React from "react";

import dark_mode_icon from "../../../../assets/icons/dark_mode.svg";
import { IconButton } from "../../../base";

function ThemeToggle() {
  return <IconButton icon={dark_mode_icon} />;
}

export default ThemeToggle;
